# Documentation map

Root documents define the product constitution and engineering standards. `adr/` records durable architectural decisions, `architecture/` holds future-state diagrams and narratives, `api/` holds API contracts, and `decisions/` is reserved for future implementation notes that do not rise to ADR level.
