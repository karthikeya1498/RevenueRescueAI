# Backend

FastAPI service for the RevenueRescue AI foundation. Start it from the repository root with `uvicorn app.main:app --app-dir backend --reload`. The `/health` endpoint is deterministic and deliberately independent of external providers.

The package directories are architectural placeholders for later phases. They must not be populated with speculative agent behavior during Phase 1.
