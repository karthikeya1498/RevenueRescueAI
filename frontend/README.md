# Frontend

Minimal React + Vite + TypeScript foundation. Run `npm install`, then `npm run dev`; use `npm run build` for production-build verification. The future dashboard belongs under `src` in feature-oriented components, while business rules remain in the backend.
